repos:
  - repo: local
    hooks:
      - id: unit-tests-with-coverage
        name: Unit tests with coverage above 90%
        entry: uv run pytest tests/unit --cov --cov-report=term-missing
        language: system
        pass_filenames: false
        always_run: true
